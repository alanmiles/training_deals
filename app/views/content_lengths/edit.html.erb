<% provide(:title, 'Edit content length') %>
<h1>Edit content length</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@length) do |f| %>

		 	<%= render 'fields', f: f %>

			<%= f.submit "Confirm", class: "btn btn-primary" %>
		<% end %>
	</div>
</div> 
<div class="navigator">
	<%= link_to "<- Cancel", content_lengths_path %>
</div>