<% provide(:title, 'New content length') %>
<h1>New content length</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@length) do |f| %>

		 	<%= render 'fields', f: f %>

			<%= f.submit "Create", class: "btn btn-primary" %>
		<% end %>
	</div>
</div> 
<div class="navigator">
	<%= link_to "<- All content lengths", content_lengths_path %>
</div>